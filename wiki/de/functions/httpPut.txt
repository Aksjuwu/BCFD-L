<dash>
name: $httpPut
desc: Sendet eine HTTP-PUT-Anfrage an eine URL mit optionalem Body.
category: HTTP

<block type="text" title="Wie funktioniert der Befehl?">
text: $httpPut sendet eine PUT-Anfrage mit einem Timeout von 10 Sekunden. Der Body wird als JSON gesendet, wenn er gültiges JSON ist, andernfalls als Rohtext. Die Antwort wird in $httpResult und der Statuscode in $httpStatus gespeichert. Mit $httpAddHeader hinzugefügte Header werden gesendet und anschließend gelöscht.
</block>

<block type="code" title="Syntax">
text: $httpPut[URL; (Body)]
</block>

<block type="code" title="Kopierfertiges Beispiel">
text: $httpAddHeader[Content-Type; application/json]
$httpPut[https://api.example.com/users/1; {"name": "FDScript"}]
$httpStatus
</block>

<block type="callout" kind="note">
text: Ist der Body gültiges JSON, wird er als JSON gesendet, andernfalls als Rohdaten. Bei einem Fehler wird der Status auf 500 gesetzt und $httpResult enthält die Fehlermeldung.
</block>
</dash>

<details>
syntax: $httpPut[URL; (Body)]
param: URL|text|Erforderlich|Die URL, an die die PUT-Anfrage gesendet werden soll
param: Body|text|Optional|Der Anfrage-Body (JSON oder Rohtext)
</details>
