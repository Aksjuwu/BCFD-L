<dash>
name: $httpPost
desc: Sends an HTTP POST request to a URL with an optional body.
category: HTTP

<block type="text" title="How does the command work?">
text: $httpPost sends a POST request with a 10 second timeout. The body is sent as JSON when it is valid JSON, otherwise as raw text. The response is stored in $httpResult and the status code in $httpStatus. Headers added with $httpAddHeader are sent and then cleared.
</block>

<block type="code" title="Syntax">
text: $httpPost[URL; (Body)]
</block>

<block type="code" title="Ready-to-copy Example">
text: $httpAddHeader[Content-Type; application/json]
$httpPost[https://api.example.com/users; {"name": "FDScript"}]
$httpResult[id]
</block>

<block type="callout" kind="note">
text: If the body is valid JSON it is sent as JSON, otherwise it is sent as raw data. On failure the status is set to 500 and $httpResult contains the error message.
</block>
</dash>

<details>
syntax: $httpPost[URL; (Body)]
param: URL|text|Required|The URL to send the POST request to
param: Body|text|Optional|The request body (JSON or raw text)
</details>
